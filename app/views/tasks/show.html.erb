<h1>家計簿詳細</h1>
<table class="table table-striped">
  <tr>
    <th>名前</th>
    <td><%= @task.name %></td>
  </tr>
  <tr>
    <th>内容</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>完了済み？</th>
    <% if @task.complete then %>
    <td>完了済み</td>
    <% else %>
    <td>未完了</td>
    <% end %> 
  </tr>
</table>
<%= link_to "戻る", tasks_path, class:"btn btn-secondary" %>